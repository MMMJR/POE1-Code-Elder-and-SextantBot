<UserControl x:Class="Archnemesis.Gui"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Archnemesis"
             xmlns:settingsPath="clr-namespace:Archnemesis"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="600">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="5"
                    HorizontalAlignment="Left"
                    VerticalAlignment="Top"
                    Orientation="Vertical"
                    DataContext="{x:Static settingsPath:ArchnemesisSettings.Instance}">
            
            <GroupBox Header="Settings">
                <StackPanel Margin="5"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Top"
                            Orientation="Vertical">
                    <TextBlock TextWrapping="WrapWithOverflow"
                               Text="Attention: Make sure your bot can kill the build mobs. Otherwise it might loose &br;them as it forgets about them. Without automatic building it will still collect all basic parts." />
                    <CheckBox Margin="5" Content="Automatically combine recipes"
                              IsChecked="{Binding Path=DoCombine}" />
                    <CheckBox Margin="5" Content="Activate &quot;Remnants&quot;"
                              ToolTip="The tangle and red altars with the two options each"
                              IsChecked="{Binding Path=ActivateRemnants}" />
                    <CheckBox Margin="5" Content="Pickup Opulent parts"
                              IsChecked="{Binding Path=PickupOpulent}" />
                    <CheckBox Margin="5" Content="Verbose logging"
                              IsChecked="{Binding Path=VerboseLogging}" />

                </StackPanel>
            </GroupBox>

        </StackPanel>
    </ScrollViewer>
</UserControl>